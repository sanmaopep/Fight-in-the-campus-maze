#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../doxmate/bin/doxmate-theme" "$@"
  ret=$?
else 
  node  "$basedir/../doxmate/bin/doxmate-theme" "$@"
  ret=$?
fi
exit $ret

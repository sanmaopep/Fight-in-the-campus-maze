#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../doxmate/bin/doxmate" "$@"
  ret=$?
else 
  node  "$basedir/../doxmate/bin/doxmate" "$@"
  ret=$?
fi
exit $ret
